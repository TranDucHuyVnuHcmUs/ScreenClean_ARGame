
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// This class is to mark the states where we have specific, static, hard-coded renderer (for example, the last level).
/// </summary>
[CreateAssetMenu(fileName = "GameLastLevelPlayState", menuName = "Game/States/Last level play")]
public class GameLastLevelPlayState: GameState
{
}